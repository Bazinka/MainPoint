<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TransparentBackground" parent="AppTheme">
        <!-- подлупрозрачность окна -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- прозрачный цвет бэкграунда -->
        <item name="android:windowBackground">@color/transparency_grey</item>
        <!-- отключаем оверлей содержимого окна -->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item> <!-- отключаем заголовок -->
        <item name="android:windowIsFloating">true</item> <!-- окно в любой части экрана -->
        <item name="android:backgroundDimEnabled">false</item> <!-- отключаем затемнение -->
    </style>
</resources>
